<% flash.each do |key, value| %>
    <div class="col-sm-offset-4 col-sm-4 text-center <%=signed_in? ? "flash-list" : "unsigned-flash-list"%> alert alert-<%=key%>" >
    	<%= value %>
    </div>
  <% end %>